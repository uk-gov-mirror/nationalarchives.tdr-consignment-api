library("tdr-jenkinslib")

standardDeployJob(
    IMAGE_NAME: "consignment-api",
    TO_DEPLOY: params.TO_DEPLOY,
    ECS_SERVICE: "consignmentapi",
    STAGE: params.STAGE,
    TEST_DELAY_SECONDS: 300
)
